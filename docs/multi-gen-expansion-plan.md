# Multi-Generation Expansion Plan

## Goal

End state: full Gen 1-9 dex support, with the ability to view any single generation's data (starting with Gen 3 / FRLG-RSE, the current baseline) independent of which generations are enabled overall — a "classic mode" toggle that never goes away as later gens are added.

## Current state (baseline)

- Dex #1-386 (Gen 1-3) only, defined by `GENERATIONS` / `DEFAULT_ENABLED_GENERATIONS` in `src/data/constants.ts`.
- Movepool filtered to `firered-leafgreen` + `emerald` version groups (`MOVE_VERSION_GROUPS`).
- Flavor text: 4 tabs, Gen 3 games only (`FLAVOR_TEXT_TABS`).
- Evolution conditions cover every Gen 1-3 method: level, item, trade, trade + held item, friendship, friendship + time-of-day, beauty, relative physical stats. (Fixed 2026-07-15 — see Log.)
- No regional forms / Mega Evolution / Gigantamax / alternate varieties — one species = one dex row, 1:1.

## Resolved — "Active Gen" setting, global, independent of dex-range filter

**Mechanism**: a single-select "Active Gen" control (not a binary Gen3-vs-latest toggle) — pick any generation the app currently supports, and stats/moves/flavor text everywhere prioritize that generation's data, falling back to the latest supported generation's data when the active generation has no divergent record for a given species. Lives in the plugin's Settings tab, not an in-view control — see the 2026-07-15 "relocated to Settings" log entry for why. The main view shows the current choice passively, as `Pokedex • Gen N` in place of the plain `Pokedex` title.

**Stats**: `/pokemon/{id}.stats` only returns the **current** (latest-game) base stat line — there's no per-version-group stats field the way movepools have `version_group_details`. But most species' current stats *are* what their era actually used; the only known divergence so far is the list of species Gen 6 (X/Y) buffed (e.g. Butterfree, Beedrill, Pidgeot, Arbok, and others — exact list to verify at implementation time). Decision: a small hardcoded override table, same pattern as `FOSSIL_IDS` in `constants.ts` — `{ pokemonId: { validThroughGen: N, stats: {...} } }`. Lookup: if Active Gen ≤ `validThroughGen` for that species, use the override; otherwise fall back to current-gen `/pokemon` stats. For every species not in the table, the fallback already *is* correct data for every generation (nothing ever changed) — not a compromise. Extend the table whenever a later phase introduces a further divergence (Gen 6 itself will, once Phase 3 lands).

**Moves/flavor text**: same Active Gen selection drives which generation's version-group tabs show (the existing FRLG/RSE and per-game flavor tabs are the *game* level, nested one level below the *generation* Active Gen picks — Gen 4 gets its own Diamond/Pearl-Platinum-HGSS tabs under "Gen 4", same pattern, not a flat new tab row bolted alongside FRLG/RSE).

**Scope — independent of the existing row-visibility filter**: Active Gen is a separate axis from the existing multi-select `GENERATIONS` filter (which controls which dex rows/Pokemon are visible at all). Picking "Gen 3" as Active Gen does not restrict the dex range — you can browse a Gen 7 Pokemon while Active Gen is set to Gen 3; its stats/moves/flavor simply fall back to latest since Gen 3 predates it. This keeps the two concerns (row visibility vs. data-era preference) uncoupled.

## Design principle: generation is a first-class axis, not bolted on later

Reuse and extend existing precedent rather than inventing a new mechanism:

- `GENERATIONS` (`constants.ts`) — already a table of gen → dex range; adding Gen 4-9 entries is additive.
- `MOVE_VERSION_GROUPS` filter pattern — the same filtering approach extends to any future per-gen moveset toggle.
- `FLAVOR_TEXT_TABS` — the same tab pattern extends to new games per generation.

Any "view as Gen X" toggle should compose these four filters (dex range, moveset version group, flavor version, stat-override lookup) rather than a one-off mechanism per generation.

## Recipe: adding the next generation

Repeatable steps, extracted from how Phase 1 (Gen 4) actually got built — follow in order, each is independently testable:

1. **Dex range**: add an entry to `GENERATIONS` in `constants.ts` (id, name, start/end dex number). `DEFAULT_ENABLED_GENERATIONS` picks it up automatically.
2. **Move/flavor tables**: add the new generation's key to `MOVE_VERSION_TABS_BY_GEN` (game version-group names, e.g. `"black-white"`) and `FLAVOR_TEXT_TABS_BY_GEN` (game version names, grouping identical-text games into one tab like the existing Ruby/Sapphire and Diamond/Pearl pairs). Source names from PokeAPI's `version-group`/`version` endpoints, not memory. **This step alone re-scopes every other version-filtered field for free** — `FLAVOR_TEXT_VERSION_GROUPS` (derived from the table above) is the one shared "games this app currently supports" list, reused by held items (`normalizeHeldItemDetails`/`normalizeHeldItems`) and anything future added the same way. Don't invent a second version list for a new per-species, per-game field — filter it against this one, same as held items does.
3. **New evolution conditions**: check whether this generation introduces evolution triggers not yet modeled (see each phase's bullet list above — e.g. Gen 5 adds `trade_species`, Gen 6 adds `needs_overworld_rain`/`turn_upside_down`). Extend `RawEvolutionChainLink`/`EvolutionNode` in `types.ts` and `normalizeEvolutionChain`/`describeEvolutionRequirement` in `normalize.ts` — same pattern each time (raw field → normalized field → label branch). `describeEvolutionRequirement` (the evolution-card label logic, moved here from `EvolutionChain.svelte` during a 2026-07-15 architecture pass — that component is render-only now) has its own test fixtures in `normalize.test.ts` via a local `node(overrides)` helper, separate from `nextEvolutionLevels`'s inline literal `EvolutionNode` objects — a new field needs updating both. Verify live against the *actual* evolution-chain responses for this generation's species rather than trusting the phase bullet's own categorization — it's been wrong twice (see the Phase 3/5 log correction and the fix immediately above this sentence).
4. **Stat overrides**: research (don't assume from memory — verified live fetch + parse caught a real bug last time) whether this generation's release included a base-stat rebalance for any dex #1-N species already in scope. Extend `STAT_OVERRIDES` in `constants.ts` if so, using `validThroughGen` to mark the last generation still using the old value.
5. **Sprites**: spot-check that PokeAPI actually has sprite data for the new dex range (`sprites.versions['generation-<n>']` etc.) — no code change expected, this has been a non-issue so far, but confirm before assuming.
6. **Verify**: `npx tsc --noEmit`, `npx vitest run`, `npm run build`, `npm run lint`, then live-verify in the vault per this repo's CLAUDE.md gotchas (deploy → disable/enable → drive it via `obsidian-cli eval`) — at minimum, one Pokemon per new evolution condition and per new stat override, the Active Gen setting actually switching the new generation's move/flavor tabs in, and a species with wild held items to confirm nothing from the *next* out-of-scope generation leaks in (the Parasect/Balm Mushroom bug).
7. **Log it**: append a dated entry to this doc's Log section — what shipped, what was scoped out, anything non-obvious hit along the way (a wrong assumption caught, an API quirk, etc.) — same as every entry below.

**Recurring gotchas, not one-time** (hit across multiple phases, will hit again):
- A test fixture that uses a real version-group name as a "stand-in for a version group this app doesn't support yet" (e.g. `sun-moon`, `black-white`) silently breaks the moment that generation actually ships for real — grep fixtures for hardcoded version-group names after each phase and swap the stand-in to a genuinely-still-future one.
- Adding a required field to `EvolutionNode`/`PokedexTableRow` breaks every inline literal fixture across `*.test.ts` (the `node(overrides)` helper, `nextEvolutionLevels`'s inline objects, `filterPokemon.test.ts`/`sortPokemon.test.ts`'s `row()` builders, etc). A python regex bulk-patch over the literal pattern (e.g. `id: N, name: "X",` → insert the new fields) is much faster than editing each by hand — see the `dexNumber`/`formLabel`/`tradeSpecies`/`needsOverworldRain` additions for the pattern.
- A new per-phase mechanic isn't guaranteed scoped to the new dex range — PokeAPI's `species.varieties` can retroactively add a variety to an already-shipped species from an earlier phase (regional forms hit 18 Gen 1-3 species already in the table, not just the new Gen 7 range). Scan the *whole* already-supported dex range for the new variety type before assuming it's phase-local, the same way the stats-override research already does for buff waves.

## Phased rollout

### Phase 1 — Gen 4 (Sinnoh, #387-493), no forms/Mega/Gigantamax
- Add a Gen 4 entry to `GENERATIONS` (dex range 387-493).
- Add Gen 4 move-version-group entries (`diamond-pearl` / `platinum` / `heartgold-soulsilver`) as their own game-tab set, nested under Gen 4 in the Active Gen dropdown (see "Active Gen dropdown" section above).
- Add Gen 4 `FLAVOR_TEXT_TABS` entries.
- New evolution conditions introduced in Gen 4: location-based (e.g. Magneton → Magnezone), known-move, party-species/held-item combos (e.g. Mantyke). Extend `EvolutionNode`/`detailLabel` the same way the min-beauty/relative-physical-stats work did.
- Sprites: confirm PokeAPI's Gen 4 sprite paths (`sprites.versions['generation-iv']`) cover what's needed.
- No regional forms yet — none exist before Gen 5, moot for this phase.
- Resolve the stats risk above (at minimum: explicit decision + this doc updated) before calling Phase 1 done.

### Phase 2 — Gen 5 (Unova, #494-649)
- New evolution trigger: `trade_species` (Karrablast ↔ Shelmet).
- No new form complexity.

### Phase 3 — Gen 6 (Kalos, #650-721) — shipped, see Log
- Mega Evolution introduced — first real forms/varieties problem. Needs an explicit scope decision: skip entirely, or model as a toggle/badge on the base species rather than a separate dex row (Mega isn't a permanent evolution and has no natural dex number of its own).
- This is also where several Gen 1-3 species got the base-stat buffs referenced in the stats risk above.
- Two new evolution conditions also land here, not Phase 5 as originally miscategorized below: `needs_overworld_rain` (Sliggoo #705 → Goodra) and `turn_upside_down` (Inkay #686 → Malamar) — both species are dex #650-721, i.e. Gen 6 natives, verified live against their evolution-chain responses. The original Phase 5 bullet listing these against Gen 8 was wrong; corrected when Phase 3 actually shipped.

### Phase 4 — Gen 7 (Alola, #722-809) — shipped, see Log
- Regional forms (Alolan) introduced — same species, alternate `pokemon` variety/form in PokeAPI, not a new species id. Needs a decision: separate browsable rows, or a form-switcher on one row. This affects table/search/filter architecture directly, since dex id is no longer 1:1 with a table row once it ships.
- Z-Moves are a battle mechanic, not dex data — likely permanently out of scope.

### Phase 5 — Gen 8 (Galar, #810-905)
- Gigantamax — same forms problem as Mega, an additional layer on top.
- Regional forms (Galarian) — same architecture question as Alolan above.

### Phase 6 — Gen 9 (Paldea, #906-1025ish)
- Terastallization is a battle mechanic — likely out of scope, not a dex-level trait.
- Paldean forms (e.g. Tauros variants).
- Confirm final dex count, including any DLC additions.

## Explicitly deferred — needs its own go/no-go, not a default yes

Mega Evolution, Gigantamax, regional forms, and Terastallization are all real architecture changes (species stops being 1:1 with a dex row). None of these should be folded silently into a phase — each needs its own decision before work starts.

Z-Moves and Dynamax are battle-only mechanics, not dex traits, and are likely permanently out of scope for a dex reference tool.

## Open questions — none blocking Phase 1 start

All three original open questions (stats source, moveset toggle UI, global-vs-per-Pokemon scope) resolved into the single "Active Gen dropdown" design above. Remaining open items are the per-phase go/no-go decisions already called out under "Explicitly deferred" (Mega, Gigantamax, regional forms, Terastallization) — those stay pending until their respective phase is actually reached, by design.

## Log

- 2026-07-15: Gen 1-3 evolution-condition gaps fixed (friendship, friendship + time-of-day, trade + held item, beauty, relative physical stats). Baseline for future gens' evolution work.
- 2026-07-15: This plan created.
- 2026-07-15: Active Gen dropdown design agreed — global, single-select across all supported generations, independent of the existing dex-row-visibility filter; stats resolved via curated override table with fallback to latest; moves/flavor game-tabs nest under whichever generation is active.
- 2026-07-15: **Phase 1 shipped.** `GENERATIONS` extended with Gen 4 (#387-493); `MOVE_VERSION_GROUPS`/`FLAVOR_TEXT_TABS` restructured into per-generation tables (`MOVE_VERSION_TABS_BY_GEN`, `FLAVOR_TEXT_TABS_BY_GEN`); new Gen 4 evolution conditions added (location, known-move, party-species, gender — gender wasn't in the original Phase 1 bullet list but is the same category of gap as the Gen 1-3 fixes, so folded in); `STAT_OVERRIDES` curated table built from Bulbapedia's Gen 6 stat-buff list (22 species, dex #1-493 in scope, sourced via live fetch + parse rather than memory — the naive parse mis-indexed single-type rows against the Total column, caught by sanity-checking known values, fixed by anchoring from the row's end instead of a fixed offset); Active Gen wired into stats/moves/flavor text with fallback-to-latest-generation behavior verified for a genuine no-data case (Turtwig at Active Gen 3). Not done in this phase: browse-table stat columns still show current-gen stats unconditionally (Active Gen only affects the detail screen) — a known, intentionally-scoped gap, not an oversight.
- 2026-07-15: Active Gen control relocated to the Settings tab (not an in-view dropdown) — a "flip constantly mid-session" control didn't fit; opening Settings to occasionally switch eras does. The main view instead shows a passive `Pokedex • Gen N` indicator in place of the plain `Pokedex` title, read from `PluginSettings.activeGen` at each `getDisplayText()` call. This meant reverting the lightweight non-remounting persistence path built earlier in favor of the existing `saveSettings()`/full-remount convention every other setting already uses — an accepted tradeoff now that quick in-session flipping isn't the goal. Surfaced two separate, undocumented title-refresh surfaces in Obsidian's `WorkspaceLeaf`/`ItemView` (`leaf.updateHeader()` for the tab-strip chip, `containerEl.querySelector(".view-header-title")` for the larger pane title shown when focused) — verified live that a naive implementation using only `updateHeader()` left the pane title stale; both are now patched in `PokedexView.refresh()`.
- 2026-07-15: Wild-encounter held items added — a toggleable table column (name only) and a richer detail-page line (name + rarity %), from `RawPokemon.held_items`. This is data-completeness work spanning every currently-supported generation, not gen-specific, so it lives outside the phase numbering above. Two things worth remembering for the next data field like this: (1) a brand-new field on an already-cached raw response needs the `isStale`-refetch precedent (see `getOrFetchPokemon`'s `held_items`-presence check), not just a "Clear cache" suggestion — a cache-cleared session masked this the first time, only caught because a second disable/enable cycle hit genuinely-stale cache; (2) PokeAPI's per-item `version_details` must be filtered to `FLAVOR_TEXT_VERSION_GROUPS` (this app's already-supported-games list) or a later generation's item leaks into an earlier-generation-scoped view — caught live via Parasect showing Gen 5's Balm Mushroom while capped at Gen 4.
- 2026-07-15: **Phase 2 shipped.** `GENERATIONS` extended with Gen 5 (#494-649, verified live against `/generation/5` — 156 species, min 494 max 649); `MOVE_VERSION_TABS_BY_GEN`/`FLAVOR_TEXT_TABS_BY_GEN` gained a `5` entry (`black-white`/`black-2-white-2`, versions `black`/`white`/`black-2`/`white-2` — confirmed live via `/version-group/black-white` and `/version-group/black-2-white-2`, both `generation-v`). New evolution condition: `trade_species` (Karrablast <-> Shelmet) — added `trade_species: NamedApiResource | null` to `RawEvolutionChainLink.evolution_details`, `tradeSpecies: string | null` to `EvolutionNode`, normalized in `normalizeEvolutionChain`, labeled in `describeEvolutionRequirement` as a hard override (`Trade (for X)`) parallel to the existing `heldItem` override — the two are mutually exclusive in practice, confirmed via Karrablast's live evolution-chain response (`trigger: "trade"`, `trade_species: {name: "shelmet"}`, `held_item: null`). `STAT_OVERRIDES` extended with 7 Gen 5 species pulled into the Gen 6 stat-buff list (Stoutland #508, Unfezant #521, Gigalith #526, Seismitoad #537, Leavanny #542, Scolipede #545, Krookodile #553) — sourced live from Bulbapedia's Base stats page same as Phase 1, end-anchored parse (that bug already cost a debugging pass once, worth always doing it this way). Sprites confirmed present for the new range via `sprites.versions['generation-v']` spot-checks (Victini/Zorua/Genesect) — no code change needed. Fixed two test fixtures that had used `black-white`/`solar-beam-in-black-white` as stand-ins for "a version group this app doesn't support yet" (`normalize.test.ts`'s `normalizeMoveDetail` fallback test, `PokedexRepository.test.ts`'s untrimmed-cache-migration test, and the `bulbasaur.json` fixture itself) — swapped to `sun-moon` (Gen 7, still safely out of scope). Also fixed `generationFallback.test.ts`'s local mock tab tables, which hardcoded gen `4` as the "latest gen" fallback target — `resolveTabsForGen`'s `LATEST_GEN` is derived from the real `GENERATIONS` constant at import time, so once Gen 5 became real-latest the mock's key-4 entries stopped being reachable; the fix is keying the mock's "latest" entry to whatever gen `GENERATIONS` currently maxes out at, not gen-4 forever. Live-verified: 649/649 rows loaded with Gen 5 enabled, `Pokedex • Gen 5` title, Karrablast's evolution card literally reading `Trade (for shelmet)`, Krookodile's detail-page Def showing the pre-buff `70` (not live `80`) with Active Gen 5, and Krookodile's Moves panel showing a `Moves (G5)` heading with `B/W`/`B2/W2` tabs.
- 2026-07-15: Held-item hover tooltip added — a description popover on the detail page's held-item line, matching the existing ability/move hover pattern (`AbilitiesPanel`). New `PokeApiClient.fetchItem`/`RawItem`, `PokedexRepository.getItemDescription` (mem+disk cached by item name, `effect_entries` English `short_effect`, verified live against `/item/oran-berry`), and a new `HeldItemsPanel.svelte` extracted so `DetailScreen`'s `.breeding-line` wrapper (and its scoped CSS) stays in the parent — only the inner item-list markup moved, avoiding the extraction-loses-scoped-CSS gotcha. One real bug caught live: a literal `<span>, </span>` separator between items had its trailing space silently trimmed by Svelte's whitespace collapsing at the tag boundary, rendering "Item1,Item2" with no space — fixed by dropping the separator span entirely and using `.held-item-name:not(:last-child)::after { content: ", "; }` instead (verified via `getComputedStyle(el, '::after').content` since `.textContent` doesn't see CSS-generated content — screenshots alone wouldn't have caught this).
- 2026-07-15: **Phase 3 shipped** (Gen 6 baseline + Mega Evolution toggle). Baseline: `GENERATIONS` extended with Gen 6 (#650-721, verified live — 72 species, min 650 max 721); `MOVE_VERSION_TABS_BY_GEN`/`FLAVOR_TEXT_TABS_BY_GEN` gained a `6` entry (`x-y`/`omega-ruby-alpha-sapphire`, versions `x`/`y`/`omega-ruby`/`alpha-sapphire`). Two evolution conditions added — `needsOverworldRain`/`turnUpsideDown` on `EvolutionNode`, labeled as independent suffixes in `describeEvolutionRequirement` (`"Lv. 50 (Rain)"`, `"Lv. 30 (Upside-down)"`) — see the corrected Phase 3/5 bullets above for why these are Gen 6, not Gen 8 as originally planned; caught by verifying live against Sliggoo's and Inkay's actual evolution-chain responses instead of trusting the doc's own phase categorization. No new `STAT_OVERRIDES` entries needed — a species can only diverge from its current stats if it predates the Gen 6 buff wave, so nothing native to Gen 6 itself is ever eligible.
  **Mega Evolution**: scoped via an explicit grill (per this doc's own "needs a go/no-go" flag) before building anything — confirmed live that Mega varieties live under `species.varieties` as separate `/pokemon/{name}` resources (e.g. `charizard-mega-x`, PokeAPI id 10034+, `is_default: false`), never a new species or dex number, which is why `PokedexRepository`'s id/contiguous-range-based row model was ruled out as the wrong place to model it. Went with "toggle on the base species' detail page" — validated with a throwaway 3-variant HTML/CSS/JS prototype (`temp/PROTOTYPE-mega-toggle.html`, deleted after) comparing pill-tabs-under-type-row vs. an icon-badge-on-portrait-popover vs. a full-width mode banner; picked pill-tabs, placed in the identity column under the type badges (not inside the Evolution section, despite the visual-style comparison to Move's version tabs, since Mega swaps stats/abilities/sprite too, all outside Evolution's own concern). New types: `RawSpecies.varieties`, `MegaFormSummary` (`{key, label}`, cheap — derived from already-fetched species data, no extra fetch), `MegaFormDetail` (`{types, abilities, stats}` + the same four image fields as `PokedexEntry` so `DetailScreen`'s existing shiny/artwork-style derivation logic works unmodified against either shape). `deriveMegaForms` filters `species.varieties` by a `"{species}-mega"` name-prefix (excludes `-gmax` naturally, verified against Charizard/Gengar/Mewtwo's real variety lists — single-mega species use bare `-mega`, split ones use `-mega-x`/`-mega-y`). New `PokedexRepository.getMegaForm(varietyKey)` — lazy, fetched only on tab click (not prefetched for all 900+ species; <40 actually have one), disk-cached under `mega-forms/{varietyKey}.json` plus four images under `images/{varietyKey}-*.png`, keyed by the PokeAPI variety *name* so it can't collide with the numeric-id `pokemon/{id}.json` cache. `DetailScreen` owns `activeMegaKey`/`megaFormCache` state (not the new `MegaFormTabs.svelte`, which is presentational-only) since the selection has to drive sibling panels (StatBars/AbilitiesPanel/portrait), not just itself — same shape as `onBack`/`onSelect` callback props elsewhere in this view. Confirmed live that Mega varieties do have their own shiny sprites (`front_shiny`/official-artwork shiny both present via the API), so the shiny toggle composes with the Mega toggle rather than being disabled for it. Live-verified end to end on Charizard: 721/721 rows loaded, Mega tabs read "Base / Mega X / Mega Y", selecting Mega X swapped type badges to Fire/Dragon, stats to the exact live values (78/130/111/130/85/100, total 634), ability to `tough-claws`, and the portrait to a freshly-fetched sprite (shiny toggle also verified swapping to a different image while Mega X stayed active); switching back to Base reverted every field exactly; a non-Mega species (Bulbasaur) correctly shows no tabs at all.
- 2026-07-15: Mega toggle UI revised same-day after user feedback — the pill-tab bar (with an explicit "Base" tab) felt like it took up too much space. Replaced `MegaFormTabs.svelte` with `MegaFormToggle.svelte`: small icon buttons in the portrait's bottom-left corner (mirroring the existing shiny-toggle's top-right badge, same 26x26 footprint), one per Mega variety, no "Base" button at all — clicking the active one toggles back to base directly, clicking an inactive one switches to it (deactivating whichever else was active). Single-mega species (e.g. Gengar) get one "M" badge; the X/Y-split pair (Charizard, Mewtwo) get two badges, "X"/"Y", laid out left-to-right from the corner. Re-verified live on Charizard (X to Y switch correctly deactivates X; clicking active Y reverts every field to base) and Gengar (single "M" badge, confirming the single-vs-split badge-count branch).
- 2026-07-16: **Phase 4 shipped** (Gen 7 baseline + Alolan regional forms as real browsable rows). Baseline: `GENERATIONS` extended with Gen 7 (#722-809, verified live — 88 species); `MOVE_VERSION_TABS_BY_GEN`/`FLAVOR_TEXT_TABS_BY_GEN` gained a `7` entry — three version groups this time, not two: `sun-moon`/`ultra-sun-ultra-moon`/`lets-go-pikachu-lets-go-eevee`. No new evolution conditions or `STAT_OVERRIDES` needed (spot-checked Type: Null/Cosmog/Jangmo-o/Stufful chains live — all already-modeled level/happiness triggers).
  **Regional forms**: scoped via the same explicit-grill pattern as Mega (this doc's own "needs a go/no-go" flag) — but landed on the *opposite* answer, "separate browsable rows," since live data showed Alolan forms are nothing like Mega: real independent types/abilities/stats/movepool/wild-held-items (Alolan Rattata: Dark/Normal, gluttony/hustle/thick-fat, 63 moves vs base's 82, Pecha Berry), not a battle-only transform. Bigger scope surprise, caught only by scanning all 386 already-supported species live rather than trusting the phase bullet: **18 species already in the app's existing dex range** (#1-386) have an Alolan form — this was never a Gen-7-only concern, it retroactively affects Gen 1-3 rows shipped in earlier phases. Also caught live: Meowth's evolution chain has form-divergent `evolution_details` (regular Lv. 28 vs. Alolan's friendship-based one, `base_form`/`evolved_form` fields not previously modeled) — of the 18, only Meowth/Vulpix/Sandshrew actually diverge in evolution *method*; the rest just re-record an identical requirement per version_group. Two more (Exeggutor, Marowak) are region-gated rather than form-gated in-game, which PokeAPI's `evolution_details` can't disambiguate at all (both entries identical) — documented as an accepted gap rather than guessed at.
  **Architecture**: split what `PokedexTableRow.id` used to do three ways — `id` stays the raw fetch/cache/sort-tiebreak key (a variety's own PokeAPI pseudo-id, e.g. 10091, works fine as-is); new `dexNumber` is the displayed/searched/range-filtered "No. 019" (identical across a species' default row and every regional-form row it has); new `generationId` is precomputed per-row (dex-range lookup for a default row, a new curated `REGIONAL_FORMS` table — `{alola: {label: "Alolan", generationId: 7}}`, FOSSIL_IDS-style — for a regional one), since a form belongs to whichever generation actually introduced it regardless of its base dex number. `EvolutionNode` needed the identical `dexNumber`/`formLabel` split — its `id` is form-aware (a variety's own id when the chain was normalized for one), so evolution cards would otherwise display a raw PokeAPI pseudo-id as "No. XXX". `normalizeEvolutionChain` gained an optional `{formSuffix, rootId}` context threaded through its recursion: at each step it now picks whichever `evolution_details` entry's `base_form` matches the current form (falling back to the entry with no `base_form`, reproducing the original always-`[0]` behavior exactly when no context is given), and follows the matched entry's `evolved_form` to resolve the next node's exact id/form — never guessing one PokeAPI didn't confirm (the Exeggutor/Marowak case resets to the base variety instead). `PokedexRepository.getRowsForIds` now returns 0+ extra rows per base id (`deriveRegionalForms` exact-matches `"{species}-{suffix}"`, not `.endsWith()` — Raticate's `raticate-totem-alola` Totem variant literally ends in `-alola` too and would've slipped through); a new `getOrFetchPokemonVariant`/`pokemonVariantMemCache` mirrors the numeric-id path but keyed by PokeAPI name. `getEntryCore`/`getEntryExtras` had a latent id-vs-species-id assumption exposed by this: they used to fetch species by reusing the same numeric `id` as the pokemon fetch, which silently breaks for a variety id (there's no `/pokemon-species/10091`) — fixed by resolving the species id from the fetched pokemon's own embedded `species.url` instead, which happens to work uniformly for default rows too (same value either way).
  **Real bug caught only by live verification, not by any test**: after deploying, the table loaded but showed only 1 of 18 Alolan rows (Muk's) despite the repository layer visibly returning both rows correctly when called directly via `obsidian eval`. Root cause: `PokedexLoadState` (the browse-table's fetch/state class) reused `resolveGenerationScope(...).includes(row.id)` as a post-fetch row-visibility filter — but that function is a bare dex-*number*-range check, and a regional-form row's `id` (10091) falls nowhere near any real dex range, so `includes` silently returned false and dropped the row from both the `onRow` stream and the final `rows` list. Fixed by broadening `GenerationScope.includes` to take the whole row (checking `row.generationId` via the already-correct `matchesGenerations`, not re-deriving membership from `id`); `failedIds` (which are bare numbers — no row was ever built) kept the original bare-id dex-range check via `isIdInGenerations`, since a fetch failure always happens on the *base* id, never a regional variant (those fail independently inside `getRowsForIds`' own try/catch and never propagate). Added a regression test in `generationScope.test.ts` asserting `includes()` matches a regional-form-shaped row by `generationId` while ignoring its unrelated `id`/`dexNumber` — this is exactly the scenario unit tests hadn't covered, since every existing fixture up to this point had `id === dexNumber` by construction.
  Live-verified end to end: 827/827 rows (809 unique dex numbers, 18 doubled for their Alolan pair — confirmed via DOM query scoped to the actual leaf, after an initial miscount from an unscoped `document.querySelectorAll` that accidentally picked up 3 unrelated rows from a different open tab); search "19" finds both Rattata rows, search "alolan" finds only the regional one; sort-by-No. groups each pair adjacently; disabling Gen 7 (Gen 1 still on) hides Alolan Rattata while base Rattata stays, re-enabling brings it back; Alolan Vulpix's evolution card reads "Ice Stone" (not Fire Stone) linking to "Alolan Ninetales", and clicking through lands on the correct row (No. 038, Ice/Fairy — not base Ninetales' Fire typing); Alolan Meowth's card reads "Friendship" (not Lv. 28) leading to Alolan Persian.
